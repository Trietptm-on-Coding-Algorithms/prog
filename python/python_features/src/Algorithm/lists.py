
class node():
        def __init__(self, data):
                self.data = data
        def setNext(self, node):
                this.next_node = node
        def getNext(self):
                return Node
        def getData(self):
                return this.data
class linked_list():

        def __init__(self):
                self.start = None
        def getFirstNode():
                return self.start
        def append(self, data):
                current_node = self.start
                while current_node.getNext() != None:
                        current_node = current_node.getNext()
                current_node.setNext()
