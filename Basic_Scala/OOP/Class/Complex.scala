
//class will automatically assign the arguments to values
class Complex( real: Double, imaginary: Double) {
	def re() = real			//These are methods. They implicitly determine the return type
	def im() = imaginary
}
